Run the full test suite for both server and client.

1. Run PHP tests: `cd server && php artisan test --compact`
2. Run TypeScript type-check: `cd client && npm run type-check`

Report results for both. If either fails, show the errors clearly.
